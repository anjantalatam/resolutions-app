# AI Constitution

This document is the contract for autonomous or human-assisted agents working on this codebase, so always read this file before touching code.

## Frontend Implementation Guardrails

- Do not use deprecated or legacy imports
- Always use colors from global theme, if some new color is missing highlight it before adding it hexcode in css

## File System Guardrails

- Never delete files or directories from disk unless explicitly asked. Only remove from git tracking if that's what's requested.
